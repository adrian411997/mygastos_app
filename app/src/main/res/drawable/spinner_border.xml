<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/background_spinner" /> <!-- Cambia el color del fondo según tus preferencias -->
    <stroke android:color="@color/white" android:width="2dp" /> <!-- Cambia el color y el ancho del borde según tus preferencias -->
    <corners android:radius="4dp" /> <!-- Opcional: para bordes redondeados -->
</shape>